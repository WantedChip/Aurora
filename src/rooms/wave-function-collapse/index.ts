import type { RoomInstance } from '../types';
import { createMockRoom } from '../mock-room';

export const room: RoomInstance = createMockRoom('Wave Function Collapse', '#FFB800');
export default room;
