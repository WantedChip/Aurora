# Aurora — Environment Variables Example
# Aurora is a fully client-side static application.
# Sensitive keys must NEVER be placed here or in client bundles.

# Optional development server port configuration
VITE_PORT=5173

# Optional base URL for static asset deployments
# VITE_BASE_URL=/
