import type { RoomInstance } from '../types';
import { createMockRoom } from '../mock-room';

export const room: RoomInstance = createMockRoom('Metaballs & Marching Cubes', '#38BDF8');
export default room;
