import type { RoomInstance } from '../types';
import { createMockRoom } from '../mock-room';

export const room: RoomInstance = createMockRoom('Cyclic Cellular Automata', '#00FF9D');
export default room;
